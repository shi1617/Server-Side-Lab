### 1. 测试登录接口（获取 JWT Token）
POST http://localhost:8080/api/users/login
Content-Type: application/json

{
  "username": "test",
  "password": "123456"
}

### 2. 携带 Token 访问受保护接口（必须登录才能访问）
GET http://localhost:8080/api/users/1/detail
Authorization: Bearer 这里替换成你登录返回的Token
Content-Type: application/json

### 3. 不带 Token 访问（应该被拦截=401/403）
GET http://localhost:8080/api/users/1/detail
Content-Type: application/json